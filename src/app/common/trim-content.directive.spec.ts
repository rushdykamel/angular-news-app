import { TrimContentDirective } from './trim-content.directive';

describe('TrimContentDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimContentDirective();
    expect(directive).toBeTruthy();
  });
});
